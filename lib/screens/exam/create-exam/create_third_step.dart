import 'package:flutter/material.dart';

class CreateThirdStep extends StatelessWidget {
  const CreateThirdStep({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
