import 'package:flutter/material.dart';

class CreateFirstStep extends StatelessWidget {
  const CreateFirstStep({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
